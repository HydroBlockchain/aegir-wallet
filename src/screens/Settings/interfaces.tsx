import { StackScreenProps } from '@react-navigation/stack';
import { RootStackParams } from '../../interfaces/RootStackParams';

export interface SettingsParam extends StackScreenProps<RootStackParams, 'Settings'>{};