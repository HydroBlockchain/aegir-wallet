import { StackScreenProps } from '@react-navigation/stack';
import { RootStackParams } from '../../../navigation/RootStackParams';

export interface LoginRemittancesProps extends StackScreenProps<RootStackParams, 'UserRegister'>{};