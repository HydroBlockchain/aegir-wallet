import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  viewContainer: {
    paddingVertical: 20
  },
  address: {
    padding: 13,
    marginTop: 10,
  },
  wrapperQr: {
    marginTop: 50,
    borderRadius: 10,
    overflow: 'hidden'
  }
});

export default styles;