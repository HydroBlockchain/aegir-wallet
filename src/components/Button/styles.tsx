import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonGlobal: {
    padding: 13,
    width: '100%',
    display: "flex",
    alignItems: "center",
  },
  buttonTextGlobal: {},
  buttonOutlined: {
    borderWidth: 2,
  }
});

export default styles;